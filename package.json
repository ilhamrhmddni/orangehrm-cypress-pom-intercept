{
  "name": "orangehrm-cypress-pom-intercept",
  "version": "1.0.0",
  "private": true,
  "description": "E2E Automation for OrangeHRM Demo using Cypress + POM + cy.intercept",
  "scripts": {
    "test": "cypress run",
    "cy:open": "cypress open",
    "cy:run:headed": "cypress run --browser chrome --headed",
    "lint": "echo 'linting not configured'"
  },
  "devDependencies": {
    "cypress": "^13.13.1"
  }
}